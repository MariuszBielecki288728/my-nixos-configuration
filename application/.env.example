# Copy only the service contract(s) you enable into a local mode-0600 file.
# Never commit the resulting file. Non-secret topology is declared in Nix.

# Actual Discord bot (the values after ACTUAL_FILE are optional)
# DISCORD_TOKEN=REPLACE_WITH_DISCORD_BOT_TOKEN
# DISCORD_BANK_NOTIFICATION_CHANNEL=REPLACE_WITH_CHANNEL_ID
# DISCORD_RECEIPT_CHANNEL=REPLACE_WITH_CHANNEL_ID
# ACTUAL_FILE=REPLACE_WITH_ACTUAL_FILE_ID
# ACTUAL_ENCRYPTION_PASSWORD=REPLACE_WITH_BUDGET_ENCRYPTION_PASSWORD
# ACTUAL_ACCOUNT=REPLACE_WITH_ACTUAL_ACCOUNT_ID
